---
- hosts: all
  remote_user: root
  tasks:
    - name: install httpd
      yum: name=httpd
    - name: check the date on ther server
      command: date
    - name: check the ip on the server
      command: ip add

